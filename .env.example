# OpenAI — Sea Launch AI
OPENAI_API_KEY=sk-...

# 默认模型(可选)
OPENAI_MODEL=gpt-4o
OPENAI_IMAGE_MODEL=gpt-image-2
PROVIDER_MODE=seed

# demo 兜底:设为 1 时 /api/run 始终返回 contract/mock-result.json
DEMO_MOCK_ONLY=0
